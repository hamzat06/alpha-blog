<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <%= render 'shared/errors', obj: @user %>
      <%= form_with(model: @user, class: 'shadow mb-3 p-3 bg-info rounded', local: true) do |f| %>
        <div class="form-group row">
          <%= f.label :username, class: 'col-sm-2 col-form-label text-light' %>
          <div class="col-md-10">
            <%= f.text_field :username, class: 'form-control shadow rounded', placeholder: 'Enter a username' %>
          </div>
        </div>
        <div class="form-group row">
          <%= f.label :email, class: 'col-sm-2 col-form-label text-light' %>
          <div class="col-md-10">
            <%= f.email_field :email, class: 'form-control shadow rounded', placeholder: 'Enter email' %>
          </div>
        </div>
        <div class="form-group row">
          <%= f.label :password, class: 'col-sm-2 col-form-label text-light' %>
          <div class="col-md-10">
            <%= f.password_field :password, class: 'form-control shadow rounded', placeholder: 'Enter password' %>
          </div>
        </div>
        <div class="form-group row">
          <%= f.label :password_confirmation, class: 'col-sm-2 col-form-label text-light' %>
          <div class="col-md-10">
            <%= f.password_field :password_confirmation, class: 'form-control shadow rounded', placeholder: 'Confirm password' %>
          </div>
        </div>
        <div class="form-group row justify-content-center">
          <%= f.submit "Sign up", class: 'btn btn-outline-light btn-lg' %>
        </div>
      <% end %>
    </div>
    <%= link_to articles_path, class: 'font-weight-bold mb-3' do %>
      <i class="bi bi-chevron-left mr-2 text-info"></i>Back to articles listing
    <% end %>
  </div>
</div>
